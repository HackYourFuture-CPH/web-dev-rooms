import React from 'react';
import { StudentRegistration } from './StudentRegistrationPage.component';

export default {
  title: 'StudentRegistration',
};

export const StudentRegistrationPage = () => (
  <div>
    <StudentRegistration />
  </div>
);
